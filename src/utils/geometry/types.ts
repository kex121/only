export interface PaddingsResult {
  leftPadding: number;
  rightPadding: number;
}

export interface GeometryResult {
  centerX: number;
  centerY: number;
  minSize: number;
  circleRadius: number;
}
